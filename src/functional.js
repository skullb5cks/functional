// import _ from 'underscore';
//
// function completement(pred) {
//     return function() {
//         return !pred.apply(null, _.toArray(arguments));
//     };
// }

if (module.hot) {
    module.hot.accept();
}
